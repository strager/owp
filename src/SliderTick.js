define('SliderTick', [ ], function () {
    var SliderTick = function (time, x, y, slider) {
        this.time = time;
        this.x = x;
        this.y = y;
        this.slider = slider;
    };

    return SliderTick;
});
